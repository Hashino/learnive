[package]
name = "learnive"
version = "0.1.0"
edition = "2024"

[dependencies]
learnive-core = { path = "../core" }
axum = "0.8"
tokio = { version = "1", features = ["full"] }
tokio-stream = "0.1"
rand = "0.8"

[dev-dependencies]
tower = { version = "0.5", features = ["util"] }
